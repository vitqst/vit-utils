# Add UUID v7

Status: completed 2026-07-25.

## Goal

Add RFC 9562 UUID v7 generation alongside the existing UUID v4, ULID, and
NanoID options.

## Work

- Add failing unit coverage for the UUID v7 timestamp, version, and variant
  fields.
- Add failing UI coverage for selecting and generating UUID v7 values.
- Implement browser-local UUID v7 generation with Web Crypto randomness.
- Add UUID v7 to the English/Vietnamese product documentation and tool UI.
- Run focused tests, then the full repository check.

## Verification

- `npm test -- src/tools/ids/ids.test.ts src/tools/ids/IdsTool.test.tsx`
- `npm run check`
